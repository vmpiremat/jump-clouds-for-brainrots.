--// Full Enhanced Teleport GUI - Fixed Instant Proximity
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local player = Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

player.CharacterAdded:Connect(function(char)
    character = char
end)

-- Variables
local minimized = false
local minimizeKey = Enum.KeyCode.RightControl

local instantProximity = false
local infiniteJump = false
local noclip = false
local flying = false
local flySpeed = 60
local walkspeed = 16

local jumpConnection = nil
local noclipConnection = nil
local bodyVelocity = Instance.new("BodyVelocity")
local bodyGyro = Instance.new("BodyGyro")

bodyVelocity.MaxForce = Vector3.new(1e5, 1e5, 1e5)
bodyGyro.MaxTorque = Vector3.new(1e5, 1e5, 1e5)

-- GUI
local gui = Instance.new("ScreenGui")
gui.Name = "TeleportGUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 380, 0, 520)
mainFrame.Position = UDim2.new(0.5, -190, 0.5, -260)
mainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = false
mainFrame.Parent = gui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 12)
mainCorner.Parent = mainFrame

-- Title Bar (Draggable)
local titleBar = Instance.new("Frame")
titleBar.Size = UDim2.new(1, 0, 0, 40)
titleBar.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
titleBar.Parent = mainFrame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -100, 1, 0)
title.BackgroundTransparency = 1
title.Text = "Jump for Brainrots"
title.Font = Enum.Font.GothamBold
title.TextScaled = true
title.TextColor3 = Color3.new(1, 1, 1)
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = titleBar

-- Dragging Logic
local dragging = false
local dragStart
local startPos

titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = mainFrame.Position
    end
end)

titleBar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = false
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        mainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Side Tabs
local tabFrame = Instance.new("Frame")
tabFrame.Size = UDim2.new(0, 110, 1, -40)
tabFrame.Position = UDim2.new(0, 0, 0, 40)
tabFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
tabFrame.Parent = mainFrame

local contentFrame = Instance.new("Frame")
contentFrame.Size = UDim2.new(1, -110, 1, -40)
contentFrame.Position = UDim2.new(0, 110, 0, 40)
contentFrame.BackgroundTransparency = 1
contentFrame.Parent = mainFrame

local function createTab(name, yPos)
    local tab = Instance.new("TextButton")
    tab.Size = UDim2.new(1, 0, 0, 50)
    tab.Position = UDim2.new(0, 0, 0, yPos)
    tab.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
    tab.Text = name
    tab.Font = Enum.Font.GothamSemibold
    tab.TextColor3 = Color3.new(1,1,1)
    tab.TextScaled = true
    tab.Parent = tabFrame

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = tab

    return tab
end

local teleTab = createTab("Teleports", 10)
local togTab = createTab("Toggles", 70)
local movTab = createTab("Movement", 130)
local setTab = createTab("Settings", 190)

-- Content Scrolling
local teleScroll = Instance.new("ScrollingFrame")
teleScroll.Size = UDim2.new(1,0,1,0)
teleScroll.BackgroundTransparency = 1
teleScroll.ScrollBarThickness = 6
teleScroll.Visible = true
teleScroll.Parent = contentFrame

local togScroll = Instance.new("ScrollingFrame")
togScroll.Size = UDim2.new(1,0,1,0)
togScroll.BackgroundTransparency = 1
togScroll.ScrollBarThickness = 6
togScroll.Visible = false
togScroll.Parent = contentFrame

local movScroll = Instance.new("ScrollingFrame")
movScroll.Size = UDim2.new(1,0,1,0)
movScroll.BackgroundTransparency = 1
movScroll.ScrollBarThickness = 6
movScroll.Visible = false
movScroll.Parent = contentFrame

local setScroll = Instance.new("ScrollingFrame")
setScroll.Size = UDim2.new(1,0,1,0)
setScroll.BackgroundTransparency = 1
setScroll.ScrollBarThickness = 6
setScroll.Visible = false
setScroll.Parent = contentFrame

local function addList(parent)
    local list = Instance.new("UIListLayout")
    list.Padding = UDim.new(0, 8)
    list.SortOrder = Enum.SortOrder.LayoutOrder
    list.Parent = parent
end

addList(teleScroll)
addList(togScroll)
addList(movScroll)
addList(setScroll)

-- Functions
local function createButton(parent, text, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -20, 0, 42)
    btn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    btn.Text = text
    btn.Font = Enum.Font.Gotham
    btn.TextColor3 = Color3.new(1,1,1)
    btn.TextScaled = true
    btn.Parent = parent

    local c = Instance.new("UICorner")
    c.CornerRadius = UDim.new(0, 8)
    c.Parent = btn

    btn.MouseButton1Click:Connect(callback)
end

-- Teleports
createButton(teleScroll, "🏠 Home", function()
    local root = character:FindFirstChild("HumanoidRootPart")
    if root then
        local plotsFolder = workspace:FindFirstChild("Plots")
        if plotsFolder then
            print("Plots folder found. Checking Plots 1-5...")
            for i = 1, 5 do
                local plot = plotsFolder:FindFirstChild(tostring(i))
                if plot then
                    print("Found Plot " .. i)
                    local ownerValue = plot:FindFirstChild("Owner")
                    if ownerValue then
                        print("Owner Value found: " .. tostring(ownerValue.Value))
                        if ownerValue.Value == player.Name then
                            print("This is your plot! Teleporting...")
                            local plotRoot = plot:FindFirstChild("HumanoidRootPart") or plot:FindFirstChildWhichIsA("BasePart")
                            if plotRoot then
                                root.CFrame = plotRoot.CFrame + Vector3.new(0, 5, 0)
                                print("Teleported successfully!")
                                return
                            else
                                print("No root part found in plot")
                            end
                        end
                    else
                        print("No Owner Value in Plot " .. i)
                    end
                else
                    print("Plot " .. i .. " not found")
                end
            end
            print("No matching plot found. Using fallback.")
        else
            print("No Plots folder found!")
        end
        -- Fallback
        root.CFrame = CFrame.new(-14, 6, 135.25)
    end
end)

createButton(teleScroll, "⚪ Common", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(155,7.5,-5) end end)
createButton(teleScroll, "🟢 Uncommon", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(375,97.5,-5) end end)
createButton(teleScroll, "🔵 Rare", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(595,237.5,-5) end end)
createButton(teleScroll, "🟣 Epic", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(815,457.5,-5) end end)
createButton(teleScroll, "🟡 Legendary", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(1035,717.5,-5) end end)
createButton(teleScroll, "🔴 Mythic", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(1255,1017.5,-5) end end)
createButton(teleScroll, "👑 God", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(1475,1367.5,-5) end end)
createButton(teleScroll, "🌌 Secret", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(1695,1707.5,-5) end end)
createButton(teleScroll, "⭐ OG", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(1915,2145.5,-5) end end)
createButton(teleScroll, "💎 Prismatic", function() local r = character:FindFirstChild("HumanoidRootPart") if r then r.CFrame = CFrame.new(2135,2697.5,-5) end end)

-- Toggle Function
local function createToggle(parent, text, default, callback)
    local t = Instance.new("TextButton")
    t.Size = UDim2.new(1, -20, 0, 42)
    t.BackgroundColor3 = Color3.fromRGB(45,45,45)
    t.Text = ""
    t.Parent = parent

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = t

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.65,0,1,0)
    label.Position = UDim2.new(0,15,0,0)
    label.BackgroundTransparency = 1
    label.Text = text
    label.TextColor3 = Color3.new(1,1,1)
    label.Font = Enum.Font.Gotham
    label.TextScaled = true
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = t

    local switch = Instance.new("Frame")
    switch.Size = UDim2.new(0,48,0,26)
    switch.Position = UDim2.new(1,-65,0.5,-13)
    switch.BackgroundColor3 = Color3.fromRGB(60,60,60)
    switch.Parent = t

    local sc = Instance.new("UICorner")
    sc.CornerRadius = UDim.new(1,0)
    sc.Parent = switch

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0,22,0,22)
    knob.Position = UDim2.new(0,2,0.5,-11)
    knob.BackgroundColor3 = Color3.new(1,1,1)
    knob.Parent = switch

    local kc = Instance.new("UICorner")
    kc.CornerRadius = UDim.new(1,0)
    kc.Parent = knob

    local state = default

    local function update()
        if state then
            switch.BackgroundColor3 = Color3.fromRGB(0,170,100)
            knob.Position = UDim2.new(1,-24,0.5,-11)
        else
            switch.BackgroundColor3 = Color3.fromRGB(60,60,60)
            knob.Position = UDim2.new(0,2,0.5,-11)
        end
    end

    t.MouseButton1Click:Connect(function()
        state = not state
        update()
        callback(state)
    end)
    update()
end

createToggle(togScroll, "Instant Proximity", false, function(v) 
    instantProximity = v 
    print("Instant Proximity " .. (v and "ENABLED" or "DISABLED"))
end)
createToggle(togScroll, "Infinite Jump", false, function(v)
    infiniteJump = v
    if v then
        jumpConnection = UserInputService.JumpRequest:Connect(function()
            local h = character:FindFirstChild("Humanoid")
            if h then h:ChangeState(Enum.HumanoidStateType.Jumping) end
        end)
    else
        if jumpConnection then jumpConnection:Disconnect() end
    end
end)
createToggle(togScroll, "Noclip", false, function(v)
    noclip = v
    if v then
        noclipConnection = RunService.Stepped:Connect(function()
            if character then
                for _, p in pairs(character:GetDescendants()) do
                    if p:IsA("BasePart") then p.CanCollide = false end
                end
            end
        end)
    else
        if noclipConnection then noclipConnection:Disconnect() end
        if character then
            for _, p in pairs(character:GetDescendants()) do
                if p:IsA("BasePart") then p.CanCollide = true end
            end
        end
    end
end)

-- Slider Function
local function createSlider(parent, title, minVal, maxVal, defaultVal, callback)
    local sFrame = Instance.new("Frame")
    sFrame.Size = UDim2.new(1, -20, 0, 60)
    sFrame.BackgroundColor3 = Color3.fromRGB(45,45,45)
    sFrame.Parent = parent

    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = sFrame

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1,0,0,20)
    label.BackgroundTransparency = 1
    label.Text = title .. ": " .. defaultVal
    label.TextColor3 = Color3.new(1,1,1)
    label.Font = Enum.Font.Gotham
    label.TextScaled = true
    label.Parent = sFrame

    local track = Instance.new("Frame")
    track.Size = UDim2.new(1, -20, 0, 8)
    track.Position = UDim2.new(0, 10, 0, 35)
    track.BackgroundColor3 = Color3.fromRGB(70,70,70)
    track.Parent = sFrame

    local tCorner = Instance.new("UICorner")
    tCorner.CornerRadius = UDim.new(1,0)
    tCorner.Parent = track

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new(0.5,0,1,0)
    fill.BackgroundColor3 = Color3.fromRGB(0,170,255)
    fill.Parent = track

    local fCorner = Instance.new("UICorner")
    fCorner.CornerRadius = UDim.new(1,0)
    fCorner.Parent = fill

    local knob = Instance.new("TextButton")
    knob.Size = UDim2.new(0,16,0,16)
    knob.Position = UDim2.new(0.5,-8,0.5,-8)
    knob.BackgroundColor3 = Color3.new(1,1,1)
    knob.Text = ""
    knob.Parent = track

    local kCorner = Instance.new("UICorner")
    kCorner.CornerRadius = UDim.new(1,0)
    kCorner.Parent = knob

    local value = defaultVal
    local dragging = false

    local function updateSlider(percent)
        percent = math.clamp(percent, 0, 1)
        fill.Size = UDim2.new(percent, 0, 1, 0)
        knob.Position = UDim2.new(percent, -8, 0.5, -8)
        value = math.floor(minVal + (maxVal - minVal) * percent)
        label.Text = title .. ": " .. value
        callback(value)
    end

    knob.MouseButton1Down:Connect(function() dragging = true end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
    end)

    RunService.RenderStepped:Connect(function()
        if dragging then
            local mousePos = UserInputService:GetMouseLocation()
            local trackPos = track.AbsolutePosition
            local trackSize = track.AbsoluteSize
            local percent = (mousePos.X - trackPos.X) / trackSize.X
            updateSlider(percent)
        end
    end)

    local initial = (defaultVal - minVal) / (maxVal - minVal)
    updateSlider(initial)
end

-- Movement Tab
createSlider(movScroll, "WalkSpeed", 16, 200, walkspeed, function(val)
    walkspeed = val
    if character and character:FindFirstChild("Humanoid") then
        character.Humanoid.WalkSpeed = val
    end
end)

createToggle(movScroll, "Fly", false, function(v)
    flying = v
    local root = character:FindFirstChild("HumanoidRootPart")
    local hum = character:FindFirstChild("Humanoid")
    if v and root and hum then
        bodyVelocity.Parent = root
        bodyGyro.Parent = root
        hum.PlatformStand = true
    else
        bodyVelocity.Parent = nil
        bodyGyro.Parent = nil
        if hum then hum.PlatformStand = false end
    end
end)

createSlider(movScroll, "Fly Speed", 20, 300, flySpeed, function(val)
    flySpeed = val
end)

-- Settings Tab
local keyLabel = Instance.new("TextLabel")
keyLabel.Size = UDim2.new(1, -20, 0, 40)
keyLabel.BackgroundTransparency = 1
keyLabel.Text = "Minimize Key: RightControl"
keyLabel.TextColor3 = Color3.new(1,1,1)
keyLabel.Font = Enum.Font.Gotham
keyLabel.TextScaled = true
keyLabel.Parent = setScroll

local changeBtn = Instance.new("TextButton")
changeBtn.Size = UDim2.new(1, -20, 0, 42)
changeBtn.BackgroundColor3 = Color3.fromRGB(45,45,45)
changeBtn.Text = "Click to Change Minimize Key"
changeBtn.Font = Enum.Font.Gotham
changeBtn.TextColor3 = Color3.new(1,1,1)
changeBtn.TextScaled = true
changeBtn.Parent = setScroll

changeBtn.MouseButton1Click:Connect(function()
    changeBtn.Text = "Press any key..."
    local conn = UserInputService.InputBegan:Connect(function(input)
        if input.KeyCode ~= Enum.KeyCode.Unknown then
            minimizeKey = input.KeyCode
            keyLabel.Text = "Minimize Key: " .. input.KeyCode.Name
            changeBtn.Text = "Click to Change Minimize Key"
            conn:Disconnect()
        end
    end)
end)

-- Tab Switching
teleTab.MouseButton1Click:Connect(function() teleScroll.Visible = true; togScroll.Visible = false; movScroll.Visible = false; setScroll.Visible = false end)
togTab.MouseButton1Click:Connect(function() teleScroll.Visible = false; togScroll.Visible = true; movScroll.Visible = false; setScroll.Visible = false end)
movTab.MouseButton1Click:Connect(function() teleScroll.Visible = false; togScroll.Visible = false; movScroll.Visible = true; setScroll.Visible = false end)
setTab.MouseButton1Click:Connect(function() teleScroll.Visible = false; togScroll.Visible = false; movScroll.Visible = false; setScroll.Visible = true end)

-- Mini Button
local miniButton = Instance.new("TextButton")
miniButton.Size = UDim2.new(0, 65, 0, 65)
miniButton.Position = UDim2.new(0.5, -32.5, 0.8, 0)
miniButton.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
miniButton.Text = "TP"
miniButton.Font = Enum.Font.GothamBold
miniButton.TextScaled = true
miniButton.TextColor3 = Color3.new(1, 1, 1)
miniButton.Visible = false
miniButton.Draggable = true
miniButton.Parent = gui

local miniCorner = Instance.new("UICorner")
miniCorner.CornerRadius = UDim.new(1, 0)
miniCorner.Parent = miniButton

local function toggleGUI()
    minimized = not minimized
    mainFrame.Visible = not minimized
    miniButton.Visible = minimized
end

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == minimizeKey then
        toggleGUI()
    end
end)

miniButton.MouseButton1Click:Connect(toggleGUI)

-- Instant Proximity Prompts (Fixed)
RunService.Heartbeat:Connect(function()
    if instantProximity then
        for _, v in ipairs(workspace:GetDescendants()) do
            if v:IsA("ProximityPrompt") then
                v.HoldDuration = 0
            end
        end
    end
end)

-- Fly Logic
RunService.RenderStepped:Connect(function()
    if not flying then return end
    local root = character:FindFirstChild("HumanoidRootPart")
    if not root then return end

    local cam = workspace.CurrentCamera.CFrame
    local dir = Vector3.new()

    if UserInputService:IsKeyDown(Enum.KeyCode.W) then dir += cam.LookVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.S) then dir -= cam.LookVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.A) then dir -= cam.RightVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.D) then dir += cam.RightVector end
    if UserInputService:IsKeyDown(Enum.KeyCode.Space) then dir += Vector3.new(0,1,0) end
    if UserInputService:IsKeyDown(Enum.KeyCode.LeftControl) then dir -= Vector3.new(0,1,0) end

    if dir.Magnitude > 0 then dir = dir.Unit end

    bodyVelocity.Velocity = dir * flySpeed
    bodyGyro.CFrame = cam
end)

print("✅ Instant Proximity Prompts should now work!")
